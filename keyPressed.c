//includes

int keyPressed(key){
    //
    int motorCmd = [0,0,0,0,0]; //init motor array
    switch(key){
        case "UPARROW":
            // motor["corresponding key"] = value;
        case "DOWNARROW":
            //do this
            //etc, will change using gamepad
    };
    return motorCmd;
};
